# Example .env snippet for loading JWT keys from PEM files.
# Why this is useful:
# - Avoids pasting large base64 strings into .env.
# - Keeps private key local file-based and out of git.
# - Public key can still be exposed via /.well-known/jwks.json for verification.

AUTH_MODE=JWKS
AUTH_ALLOW_EPHEMERAL_KEYS=false
AUTH_JWT_PRIVATE_KEY_PATH=server/keys/jwt_private.pem
AUTH_JWT_PUBLIC_KEY_PATH=server/keys/jwt_public.pem
AUTH_JWT_PRIVATE_KEY_BASE64=
AUTH_JWT_PUBLIC_KEY_BASE64=
